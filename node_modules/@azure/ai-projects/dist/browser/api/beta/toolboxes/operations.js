// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { toolUnionArraySerializer, apiErrorResponseDeserializer, toolboxPoliciesSerializer, toolboxVersionObjectDeserializer, toolboxObjectDeserializer, _agentsPagedResultToolboxObjectDeserializer, _agentsPagedResultToolboxVersionObjectDeserializer, toolboxSkillUnionArraySerializer, } from "../../../models/models.js";
import { buildPagedAsyncIterator } from "../../../static-helpers/pagingHelpers.js";
import { expandUrlTemplate } from "../../../static-helpers/urlTemplate.js";
import { createRestError, operationOptionsToRequestParameters } from "@azure-rest/core-client";
export function _deleteVersionSend(context, name, version, options = { requestOptions: {} }) {
    const foundryFeatures = "Toolboxes=V1Preview";
    const path = expandUrlTemplate("/toolboxes/{name}/versions/{version}{?api-version}", {
        name: name,
        version: version,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).delete({
        ...operationOptionsToRequestParameters(options),
        headers: { "foundry-features": foundryFeatures, ...options.requestOptions?.headers },
    });
}
export async function _deleteVersionDeserialize(result) {
    const expectedStatuses = ["204"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return;
}
/** Delete a specific version of a toolbox. */
export async function deleteVersion(context, name, version, options = { requestOptions: {} }) {
    const result = await _deleteVersionSend(context, name, version, options);
    return _deleteVersionDeserialize(result);
}
export function _$deleteSend(context, name, options = { requestOptions: {} }) {
    const foundryFeatures = "Toolboxes=V1Preview";
    const path = expandUrlTemplate("/toolboxes/{name}{?api-version}", {
        name: name,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).delete({
        ...operationOptionsToRequestParameters(options),
        headers: { "foundry-features": foundryFeatures, ...options.requestOptions?.headers },
    });
}
export async function _$deleteDeserialize(result) {
    const expectedStatuses = ["204"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return;
}
/** Delete a toolbox and all its versions. */
export async function $delete(context, name, options = { requestOptions: {} }) {
    const result = await _$deleteSend(context, name, options);
    return _$deleteDeserialize(result);
}
export function _updateSend(context, name, defaultVersion, options = { requestOptions: {} }) {
    const foundryFeatures = "Toolboxes=V1Preview";
    const path = expandUrlTemplate("/toolboxes/{name}{?api-version}", {
        name: name,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).patch({
        ...operationOptionsToRequestParameters(options),
        contentType: "application/json",
        headers: {
            "foundry-features": foundryFeatures,
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
        body: { default_version: defaultVersion },
    });
}
export async function _updateDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return toolboxObjectDeserializer(result.body);
}
/** Update a toolbox to point to a specific version. */
export async function update(context, name, defaultVersion, options = { requestOptions: {} }) {
    const result = await _updateSend(context, name, defaultVersion, options);
    return _updateDeserialize(result);
}
export function _getVersionSend(context, name, version, options = { requestOptions: {} }) {
    const foundryFeatures = "Toolboxes=V1Preview";
    const path = expandUrlTemplate("/toolboxes/{name}/versions/{version}{?api-version}", {
        name: name,
        version: version,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).get({
        ...operationOptionsToRequestParameters(options),
        headers: {
            "foundry-features": foundryFeatures,
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
    });
}
export async function _getVersionDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return toolboxVersionObjectDeserializer(result.body);
}
/** Retrieve a specific version of a toolbox. */
export async function getVersion(context, name, version, options = { requestOptions: {} }) {
    const result = await _getVersionSend(context, name, version, options);
    return _getVersionDeserialize(result);
}
export function _listVersionsSend(context, name, options = { requestOptions: {} }) {
    const foundryFeatures = "Toolboxes=V1Preview";
    const path = expandUrlTemplate("/toolboxes/{name}/versions{?limit,order,after,before,api-version}", {
        name: name,
        limit: options?.limit,
        order: options?.order,
        after: options?.after,
        before: options?.before,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).get({
        ...operationOptionsToRequestParameters(options),
        headers: {
            "foundry-features": foundryFeatures,
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
    });
}
export async function _listVersionsDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return _agentsPagedResultToolboxVersionObjectDeserializer(result.body);
}
/** List all versions of a toolbox. */
export function listVersions(context, name, options = { requestOptions: {} }) {
    return buildPagedAsyncIterator(context, () => _listVersionsSend(context, name, options), _listVersionsDeserialize, ["200"], {
        itemName: "data",
        apiVersion: context.apiVersion,
        nextPageRequestOptions: { headers: { "foundry-features": "Toolboxes=V1Preview" } },
        cursorFieldName: "last_id",
        hasMoreFieldName: "has_more",
    });
}
export function _listSend(context, options = { requestOptions: {} }) {
    const foundryFeatures = "Toolboxes=V1Preview";
    const path = expandUrlTemplate("/toolboxes{?limit,order,after,before,api-version}", {
        limit: options?.limit,
        order: options?.order,
        after: options?.after,
        before: options?.before,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).get({
        ...operationOptionsToRequestParameters(options),
        headers: {
            "foundry-features": foundryFeatures,
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
    });
}
export async function _listDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return _agentsPagedResultToolboxObjectDeserializer(result.body);
}
/** List all toolboxes. */
export function list(context, options = { requestOptions: {} }) {
    return buildPagedAsyncIterator(context, () => _listSend(context, options), _listDeserialize, ["200"], {
        itemName: "data",
        apiVersion: context.apiVersion,
        nextPageRequestOptions: { headers: { "foundry-features": "Toolboxes=V1Preview" } },
        cursorFieldName: "last_id",
        hasMoreFieldName: "has_more",
    });
}
export function _getSend(context, name, options = { requestOptions: {} }) {
    const foundryFeatures = "Toolboxes=V1Preview";
    const path = expandUrlTemplate("/toolboxes/{name}{?api-version}", {
        name: name,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).get({
        ...operationOptionsToRequestParameters(options),
        headers: {
            "foundry-features": foundryFeatures,
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
    });
}
export async function _getDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return toolboxObjectDeserializer(result.body);
}
/** Retrieve a toolbox. */
export async function get(context, name, options = { requestOptions: {} }) {
    const result = await _getSend(context, name, options);
    return _getDeserialize(result);
}
export function _createVersionSend(context, name, tools, options = { requestOptions: {} }) {
    const foundryFeatures = "Toolboxes=V1Preview";
    const path = expandUrlTemplate("/toolboxes/{name}/versions{?api-version}", {
        name: name,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).post({
        ...operationOptionsToRequestParameters(options),
        contentType: "application/json",
        headers: {
            "foundry-features": foundryFeatures,
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
        body: {
            description: options?.description,
            metadata: options?.metadata,
            tools: toolUnionArraySerializer(tools),
            skills: !options?.skills
                ? options?.skills
                : toolboxSkillUnionArraySerializer(options?.skills),
            policies: !options?.policies
                ? options?.policies
                : toolboxPoliciesSerializer(options?.policies),
        },
    });
}
export async function _createVersionDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return toolboxVersionObjectDeserializer(result.body);
}
/** Create a new version of a toolbox. If the toolbox does not exist, it will be created. */
export async function createVersion(context, name, tools, options = { requestOptions: {} }) {
    const result = await _createVersionSend(context, name, tools, options);
    return _createVersionDeserialize(result);
}
//# sourceMappingURL=operations.js.map