// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { promoteCandidate, getCandidateFile, getOptimizationCandidateResults, getOptimizationCandidateConfig, getOptimizationCandidate, listOptimizationCandidates, deleteOptimizationJob, cancelOptimizationJob, listOptimizationJobs, getOptimizationJob, createOptimizationJob, deleteSessionFile, listSessionFiles, downloadSessionFile, uploadSessionFile, getSessionLogStream, listSessions, stopSession, deleteSession, getSession, createSession, downloadAgentCode, createVersionFromCode, updateAgentObject, } from "../../../api/beta/agents/operations.js";
function _getBetaAgents(context) {
    return {
        promoteCandidate: (jobId, candidateId, candidateRequest, options) => promoteCandidate(context, jobId, candidateId, candidateRequest, options),
        getCandidateFile: (jobId, candidateId, path, options) => getCandidateFile(context, jobId, candidateId, path, options),
        getOptimizationCandidateResults: (jobId, candidateId, options) => getOptimizationCandidateResults(context, jobId, candidateId, options),
        getOptimizationCandidateConfig: (jobId, candidateId, options) => getOptimizationCandidateConfig(context, jobId, candidateId, options),
        getOptimizationCandidate: (jobId, candidateId, options) => getOptimizationCandidate(context, jobId, candidateId, options),
        listOptimizationCandidates: (jobId, options) => listOptimizationCandidates(context, jobId, options),
        deleteOptimizationJob: (jobId, options) => deleteOptimizationJob(context, jobId, options),
        cancelOptimizationJob: (jobId, options) => cancelOptimizationJob(context, jobId, options),
        listOptimizationJobs: (options) => listOptimizationJobs(context, options),
        getOptimizationJob: (jobId, options) => getOptimizationJob(context, jobId, options),
        createOptimizationJob: (inputs, options) => createOptimizationJob(context, inputs, options),
        deleteSessionFile: (agentName, agentSessionId, path, options) => deleteSessionFile(context, agentName, agentSessionId, path, options),
        listSessionFiles: (agentName, agentSessionId, options) => listSessionFiles(context, agentName, agentSessionId, options),
        downloadSessionFile: (agentName, agentSessionId, path, options) => downloadSessionFile(context, agentName, agentSessionId, path, options),
        uploadSessionFile: (agentName, agentSessionId, path, content, options) => uploadSessionFile(context, agentName, agentSessionId, path, content, options),
        getSessionLogStream: (agentName, agentVersion, sessionId, options) => getSessionLogStream(context, agentName, agentVersion, sessionId, options),
        listSessions: (agentName, options) => listSessions(context, agentName, options),
        stopSession: (agentName, sessionId, options) => stopSession(context, agentName, sessionId, options),
        deleteSession: (agentName, sessionId, options) => deleteSession(context, agentName, sessionId, options),
        getSession: (agentName, sessionId, options) => getSession(context, agentName, sessionId, options),
        createSession: (agentName, versionIndicator, options) => createSession(context, agentName, versionIndicator, options),
        downloadAgentCode: (agentName, options) => downloadAgentCode(context, agentName, options),
        createVersionFromCode: (agentName, codeZipSha256, content, options) => createVersionFromCode(context, agentName, codeZipSha256, content, options),
        updateAgent: (agentName, options) => updateAgentObject(context, agentName, options),
    };
}
export function _getBetaAgentsOperations(context) {
    return {
        ..._getBetaAgents(context),
    };
}
//# sourceMappingURL=index.js.map