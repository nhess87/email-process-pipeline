// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { list, get, generate } from "../../../api/beta/insights/operations.js";
function _getBetaInsights(context) {
    return {
        list: (options) => list(context, options),
        get: (insightId, options) => get(context, insightId, options),
        generate: (insight, options) => generate(context, insight, options),
    };
}
export function _getBetaInsightsOperations(context) {
    return {
        ..._getBetaInsights(context),
    };
}
//# sourceMappingURL=index.js.map