// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { apiErrorResponseDeserializer, routineTriggerUnionRecordSerializer, routineActionUnionSerializer, routineDeserializer, _agentsPagedResultRoutineRunDeserializer, routineDispatchPayloadUnionSerializer, dispatchRoutineResponseDeserializer, } from "../../../models/models.js";
import { buildPagedAsyncIterator } from "../../../static-helpers/pagingHelpers.js";
import { expandUrlTemplate } from "../../../static-helpers/urlTemplate.js";
import { createRestError, operationOptionsToRequestParameters } from "@azure-rest/core-client";
export function _dispatchSend(context, routineName, options = { requestOptions: {} }) {
    const path = expandUrlTemplate("/routines/{routine_name}:dispatch_async{?api-version}", {
        routine_name: routineName,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).post({
        ...operationOptionsToRequestParameters(options),
        contentType: "application/json",
        headers: {
            ...(options?.foundryFeatures !== undefined
                ? { "foundry-features": options?.foundryFeatures }
                : {}),
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
        body: {
            payload: !options?.payload
                ? options?.payload
                : routineDispatchPayloadUnionSerializer(options?.payload),
        },
    });
}
export async function _dispatchDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return dispatchRoutineResponseDeserializer(result.body);
}
/** Queue an asynchronous routine dispatch. */
export async function dispatch(context, routineName, options = { requestOptions: {} }) {
    const result = await _dispatchSend(context, routineName, options);
    return _dispatchDeserialize(result);
}
export function _listRunsSend(context, routineName, options = { requestOptions: {} }) {
    const path = expandUrlTemplate("/routines/{routine_name}/runs{?filter,limit,order,after,before,api-version}", {
        routine_name: routineName,
        filter: options?.filter,
        limit: options?.limit,
        order: options?.order,
        after: options?.after,
        before: options?.before,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).get({
        ...operationOptionsToRequestParameters(options),
        headers: {
            ...(options?.foundryFeatures !== undefined
                ? { "foundry-features": options?.foundryFeatures }
                : {}),
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
    });
}
export async function _listRunsDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return _agentsPagedResultRoutineRunDeserializer(result.body);
}
/** List prior runs for a routine. */
export function listRuns(context, routineName, options = { requestOptions: {} }) {
    return buildPagedAsyncIterator(context, () => _listRunsSend(context, routineName, options), _listRunsDeserialize, ["200"], { itemName: "data", apiVersion: context.apiVersion });
}
export function _$deleteSend(context, routineName, options = { requestOptions: {} }) {
    const path = expandUrlTemplate("/routines/{routine_name}{?api-version}", {
        routine_name: routineName,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).delete({
        ...operationOptionsToRequestParameters(options),
        headers: {
            ...(options?.foundryFeatures !== undefined
                ? { "foundry-features": options?.foundryFeatures }
                : {}),
            ...options.requestOptions?.headers,
        },
    });
}
export async function _$deleteDeserialize(result) {
    const expectedStatuses = ["204"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return;
}
/** Delete a routine. */
export async function $delete(context, routineName, options = { requestOptions: {} }) {
    const result = await _$deleteSend(context, routineName, options);
    return _$deleteDeserialize(result);
}
export function _listSend(context, options = { requestOptions: {} }) {
    const path = expandUrlTemplate("/routines{?limit,order,after,before,api-version}", {
        limit: options?.limit,
        order: options?.order,
        after: options?.after,
        before: options?.before,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).get({
        ...operationOptionsToRequestParameters(options),
        headers: {
            ...(options?.foundryFeatures !== undefined
                ? { "foundry-features": options?.foundryFeatures }
                : {}),
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
    });
}
export async function _listDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    const body = result.body;
    return {
        data: (body["value"] ?? body["data"] ?? []).map((item) => routineDeserializer(item)),
        first_id: body["first_id"],
        last_id: body["last_id"],
        has_more: body["has_more"] ?? false,
    };
}
/** List routines. */
export function list(context, options = { requestOptions: {} }) {
    return buildPagedAsyncIterator(context, () => _listSend(context, options), _listDeserialize, ["200"], {
        itemName: "data",
        nextLinkName: "nextLink",
        apiVersion: context.apiVersion,
    });
}
export function _disableSend(context, routineName, options = { requestOptions: {} }) {
    const path = expandUrlTemplate("/routines/{routine_name}:disable{?api-version}", {
        routine_name: routineName,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).post({
        ...operationOptionsToRequestParameters(options),
        headers: {
            ...(options?.foundryFeatures !== undefined
                ? { "foundry-features": options?.foundryFeatures }
                : {}),
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
    });
}
export async function _disableDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return routineDeserializer(result.body);
}
/** Disable a routine. */
export async function disable(context, routineName, options = { requestOptions: {} }) {
    const result = await _disableSend(context, routineName, options);
    return _disableDeserialize(result);
}
export function _enableSend(context, routineName, options = { requestOptions: {} }) {
    const path = expandUrlTemplate("/routines/{routine_name}:enable{?api-version}", {
        routine_name: routineName,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).post({
        ...operationOptionsToRequestParameters(options),
        headers: {
            ...(options?.foundryFeatures !== undefined
                ? { "foundry-features": options?.foundryFeatures }
                : {}),
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
    });
}
export async function _enableDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return routineDeserializer(result.body);
}
/** Enable a routine. */
export async function enable(context, routineName, options = { requestOptions: {} }) {
    const result = await _enableSend(context, routineName, options);
    return _enableDeserialize(result);
}
export function _getSend(context, routineName, options = { requestOptions: {} }) {
    const path = expandUrlTemplate("/routines/{routine_name}{?api-version}", {
        routine_name: routineName,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).get({
        ...operationOptionsToRequestParameters(options),
        headers: {
            ...(options?.foundryFeatures !== undefined
                ? { "foundry-features": options?.foundryFeatures }
                : {}),
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
    });
}
export async function _getDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return routineDeserializer(result.body);
}
/** Retrieve a routine. */
export async function get(context, routineName, options = { requestOptions: {} }) {
    const result = await _getSend(context, routineName, options);
    return _getDeserialize(result);
}
export function _createOrUpdateSend(context, routineName, options = { requestOptions: {} }) {
    const path = expandUrlTemplate("/routines/{routine_name}{?api-version}", {
        routine_name: routineName,
        "api-version": context.apiVersion,
    }, {
        allowReserved: options?.requestOptions?.skipUrlEncoding,
    });
    return context.path(path).put({
        ...operationOptionsToRequestParameters(options),
        contentType: "application/json",
        headers: {
            ...(options?.foundryFeatures !== undefined
                ? { "foundry-features": options?.foundryFeatures }
                : {}),
            accept: "application/json",
            ...options.requestOptions?.headers,
        },
        body: {
            description: options?.description,
            enabled: options?.enabled,
            triggers: !options?.triggers
                ? options?.triggers
                : routineTriggerUnionRecordSerializer(options?.triggers),
            action: !options?.action ? options?.action : routineActionUnionSerializer(options?.action),
        },
    });
}
export async function _createOrUpdateDeserialize(result) {
    const expectedStatuses = ["200"];
    if (!expectedStatuses.includes(result.status)) {
        const error = createRestError(result);
        error.details = apiErrorResponseDeserializer(result.body);
        throw error;
    }
    return routineDeserializer(result.body);
}
/** Create or update a routine. */
export async function createOrUpdate(context, routineName, options = { requestOptions: {} }) {
    const result = await _createOrUpdateSend(context, routineName, options);
    return _createOrUpdateDeserialize(result);
}
//# sourceMappingURL=operations.js.map