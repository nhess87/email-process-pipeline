var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var agents_exports = {};
__export(agents_exports, {
  _getAgentsOperations: () => _getAgentsOperations
});
module.exports = __toCommonJS(agents_exports);
var import_operations = require("../../api/agents/operations.js");
function _getAgents(context) {
  return {
    listVersions: (agentName, options) => (0, import_operations.listVersions)(context, agentName, options),
    deleteVersion: (agentName, agentVersion, options) => (0, import_operations.deleteVersion)(context, agentName, agentVersion, options),
    getVersion: (agentName, agentVersion, options) => (0, import_operations.getVersion)(context, agentName, agentVersion, options),
    createVersion(agentName, definitionOrManifestId, optionsOrParameterValues, options) {
      if (typeof definitionOrManifestId === "string") {
        return (0, import_operations.createAgentVersionFromManifest)(
          context,
          agentName,
          definitionOrManifestId,
          optionsOrParameterValues,
          options
        );
      }
      return (0, import_operations.createVersion)(
        context,
        agentName,
        definitionOrManifestId,
        optionsOrParameterValues
      );
    },
    list: (options) => (0, import_operations.list)(context, options),
    delete: (agentName, options) => (0, import_operations.$delete)(context, agentName, options),
    update(agentName, definitionOrManifestId, optionsOrParameterValues, options) {
      if (typeof definitionOrManifestId === "string") {
        return (0, import_operations.updateAgentFromManifest)(
          context,
          agentName,
          definitionOrManifestId,
          optionsOrParameterValues,
          options
        );
      }
      return (0, import_operations.update)(
        context,
        agentName,
        definitionOrManifestId,
        optionsOrParameterValues
      );
    },
    create(name, definitionOrManifestId, optionsOrParameterValues, options) {
      if (typeof definitionOrManifestId === "string") {
        return (0, import_operations.createAgentFromManifest)(
          context,
          name,
          definitionOrManifestId,
          optionsOrParameterValues,
          options
        );
      }
      return (0, import_operations.create)(
        context,
        name,
        definitionOrManifestId,
        optionsOrParameterValues
      );
    },
    get: (agentName, options) => (0, import_operations.get)(context, agentName, options)
  };
}
function _getAgentsOperations(context) {
  return {
    ..._getAgents(context)
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  _getAgentsOperations
});
//# sourceMappingURL=index.js.map
