var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var datasets_exports = {};
__export(datasets_exports, {
  _getDatasetsOperations: () => _getDatasetsOperations
});
module.exports = __toCommonJS(datasets_exports);
var import_operations = require("../../api/datasets/operations.js");
function _getDatasets(context, projectOptions = {}) {
  return {
    getCredentials: (name, version, options) => (0, import_operations.getCredentials)(context, name, version, options),
    pendingUpload: (name, version, pendingUploadRequest, options) => (0, import_operations.pendingUpload)(context, name, version, pendingUploadRequest, options),
    createOrUpdate: (name, version, datasetVersion, options) => (0, import_operations.createOrUpdate)(context, name, version, datasetVersion, options),
    delete: (name, version, options) => (0, import_operations.$delete)(context, name, version, options),
    get: (name, version, options) => (0, import_operations.get)(context, name, version, options),
    list: (options) => (0, import_operations.list)(context, options),
    listVersions: (name, options) => (0, import_operations.listVersions)(context, name, options),
    uploadFile: (name, version, filePath, options) => (0, import_operations.uploadFile)(context, name, version, filePath, { ...options, projectOptions }),
    uploadFolder: (name, version, folderPath, options) => (0, import_operations.uploadFolder)(context, name, version, folderPath, { ...options, projectOptions })
  };
}
function _getDatasetsOperations(context, projectOptions = {}) {
  return {
    ..._getDatasets(context, projectOptions)
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  _getDatasetsOperations
});
//# sourceMappingURL=index.js.map
