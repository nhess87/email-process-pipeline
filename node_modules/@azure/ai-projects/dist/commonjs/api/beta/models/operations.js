var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var operations_exports = {};
__export(operations_exports, {
  $delete: () => $delete,
  _$deleteDeserialize: () => _$deleteDeserialize,
  _$deleteSend: () => _$deleteSend,
  _getCredentialsDeserialize: () => _getCredentialsDeserialize,
  _getCredentialsSend: () => _getCredentialsSend,
  _getDeserialize: () => _getDeserialize,
  _getSend: () => _getSend,
  _listDeserialize: () => _listDeserialize,
  _listSend: () => _listSend,
  _listVersionsDeserialize: () => _listVersionsDeserialize,
  _listVersionsSend: () => _listVersionsSend,
  _pendingCreateVersionDeserialize: () => _pendingCreateVersionDeserialize,
  _pendingCreateVersionSend: () => _pendingCreateVersionSend,
  _pendingUploadDeserialize: () => _pendingUploadDeserialize,
  _pendingUploadSend: () => _pendingUploadSend,
  _updateDeserialize: () => _updateDeserialize,
  _updateSend: () => _updateSend,
  createFromSource: () => createFromSource,
  get: () => get,
  getCredentials: () => getCredentials,
  list: () => list,
  listVersions: () => listVersions,
  pendingCreateVersion: () => pendingCreateVersion,
  pendingUpload: () => pendingUpload,
  update: () => update
});
module.exports = __toCommonJS(operations_exports);
var import_models = require("../../../models/models.js");
var import_pagingHelpers = require("../../../static-helpers/pagingHelpers.js");
var import_urlTemplate = require("../../../static-helpers/urlTemplate.js");
var import_core_client = require("@azure-rest/core-client");
var import_storage_blob = require("@azure/storage-blob");
var import_node_fs = __toESM(require("node:fs"));
var import_node_path = __toESM(require("node:path"));
function _getCredentialsSend(context, name, version, credentialRequest, options = { requestOptions: {} }) {
  const foundryFeatures = "Models=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/models/{name}/versions/{version}/credentials{?api-version}",
    {
      name,
      version,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: (0, import_models.modelCredentialRequestSerializer)(credentialRequest)
  });
}
async function _getCredentialsDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models.datasetCredentialDeserializer)(result.body);
}
async function getCredentials(context, name, version, credentialRequest, options = { requestOptions: {} }) {
  const result = await _getCredentialsSend(context, name, version, credentialRequest, options);
  return _getCredentialsDeserialize(result);
}
function _pendingUploadSend(context, name, version, pendingUploadRequest, options = { requestOptions: {} }) {
  const foundryFeatures = "Models=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/models/{name}/versions/{version}/startPendingUpload{?api-version}",
    {
      name,
      version,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: (0, import_models.modelPendingUploadRequestSerializer)(pendingUploadRequest)
  });
}
async function _pendingUploadDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models.modelPendingUploadResponseDeserializer)(result.body);
}
async function pendingUpload(context, name, version, pendingUploadRequest, options = { requestOptions: {} }) {
  const result = await _pendingUploadSend(context, name, version, pendingUploadRequest, options);
  return _pendingUploadDeserialize(result);
}
function _pendingCreateVersionSend(context, name, version, modelVersion, options = { requestOptions: {} }) {
  const foundryFeatures = "Models=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/models/{name}/versions/{version}/createAsync{?api-version}",
    {
      name,
      version,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: (0, import_models.modelVersionSerializer)(modelVersion)
  });
}
async function _pendingCreateVersionDeserialize(result) {
  const expectedStatuses = ["202"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models._createAsyncResponseDeserializer)(result.body);
}
async function pendingCreateVersion(context, name, version, modelVersion, options = { requestOptions: {} }) {
  const result = await _pendingCreateVersionSend(context, name, version, modelVersion, options);
  return _pendingCreateVersionDeserialize(result);
}
function _updateSend(context, name, body, version, options = { requestOptions: {} }) {
  const foundryFeatures = "Models=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/models/{name}/versions/{version}{?api-version}",
    {
      name,
      version,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).patch({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/merge-patch+json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: (0, import_models.updateModelVersionRequestSerializer)(body)
  });
}
async function _updateDeserialize(result) {
  const expectedStatuses = ["201", "200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models.modelVersionDeserializer)(result.body);
}
async function update(context, name, body, version, options = { requestOptions: {} }) {
  const result = await _updateSend(context, name, body, version, options);
  return _updateDeserialize(result);
}
function _$deleteSend(context, name, version, options = { requestOptions: {} }) {
  const foundryFeatures = "Models=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/models/{name}/versions/{version}{?api-version}",
    {
      name,
      version,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).delete({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: { "foundry-features": foundryFeatures, ...options.requestOptions?.headers }
  });
}
async function _$deleteDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return;
}
async function $delete(context, name, version, options = { requestOptions: {} }) {
  const result = await _$deleteSend(context, name, version, options);
  return _$deleteDeserialize(result);
}
function _getSend(context, name, version, options = { requestOptions: {} }) {
  const foundryFeatures = "Models=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/models/{name}/versions/{version}{?api-version}",
    {
      name,
      version,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models.modelVersionDeserializer)(result.body);
}
async function get(context, name, version, options = { requestOptions: {} }) {
  const result = await _getSend(context, name, version, options);
  return _getDeserialize(result);
}
function _listSend(context, options = { requestOptions: {} }) {
  const foundryFeatures = "Models=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/models{?api-version}",
    {
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models._pagedModelVersionDeserializer)(result.body);
}
function list(context, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listSend(context, options),
    _listDeserialize,
    ["200"],
    { itemName: "value", nextLinkName: "nextLink", apiVersion: context.apiVersion ?? "v1" }
  );
}
function _listVersionsSend(context, name, options = { requestOptions: {} }) {
  const foundryFeatures = "Models=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/models/{name}/versions{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listVersionsDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models._pagedModelVersionDeserializer)(result.body);
}
function listVersions(context, name, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listVersionsSend(context, name, options),
    _listVersionsDeserialize,
    ["200"],
    { itemName: "value", nextLinkName: "nextLink", apiVersion: context.apiVersion ?? "v1" }
  );
}
function getAllFiles(dir) {
  const results = [];
  for (const entry of import_node_fs.default.readdirSync(dir)) {
    const fullPath = import_node_path.default.join(dir, entry);
    if (import_node_fs.default.statSync(fullPath).isDirectory()) {
      results.push(...getAllFiles(fullPath));
    } else {
      results.push(fullPath);
    }
  }
  return results;
}
async function createFromSource(context, name, version, source, options = {}) {
  const pollingTimeout = options.pollingTimeout ?? 3e5;
  const pollingInterval = options.pollingInterval ?? 2e3;
  const uploadResponse = await pendingUpload(context, name, version, {
    pendingUploadType: "TemporaryBlobReference"
  });
  const containerClient = new import_storage_blob.BlobServiceClient(
    uploadResponse.blobReference.credential.sasUri
  ).getContainerClient("");
  const files = getAllFiles(source);
  for (const filePath of files) {
    const blobName = import_node_path.default.relative(source, filePath).replace(/\\/g, "/");
    const data = import_node_fs.default.readFileSync(filePath);
    const blockBlobClient = containerClient.getBlockBlobClient(blobName);
    await blockBlobClient.upload(data, data.length);
  }
  await pendingCreateVersion(context, name, version, {
    blobUri: uploadResponse.blobReference.blobUri,
    weightType: options.weightType,
    baseModel: options.baseModel,
    description: options.description,
    tags: options.tags,
    name,
    version
  });
  const deadline = Date.now() + pollingTimeout;
  while (Date.now() < deadline) {
    try {
      return await get(context, name, version);
    } catch (e) {
      if (e.statusCode === 404) {
        await new Promise((resolve) => setTimeout(resolve, pollingInterval));
      } else {
        throw e;
      }
    }
  }
  throw new Error(
    `Model version '${name}@${version}' did not become available within ${pollingTimeout}ms.`
  );
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  $delete,
  _$deleteDeserialize,
  _$deleteSend,
  _getCredentialsDeserialize,
  _getCredentialsSend,
  _getDeserialize,
  _getSend,
  _listDeserialize,
  _listSend,
  _listVersionsDeserialize,
  _listVersionsSend,
  _pendingCreateVersionDeserialize,
  _pendingCreateVersionSend,
  _pendingUploadDeserialize,
  _pendingUploadSend,
  _updateDeserialize,
  _updateSend,
  createFromSource,
  get,
  getCredentials,
  list,
  listVersions,
  pendingCreateVersion,
  pendingUpload,
  update
});
//# sourceMappingURL=operations.js.map
