var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var operations_exports = {};
__export(operations_exports, {
  _cancelOptimizationJobDeserialize: () => _cancelOptimizationJobDeserialize,
  _cancelOptimizationJobSend: () => _cancelOptimizationJobSend,
  _createAgentVersionFromCodeDeserialize: () => _createAgentVersionFromCodeDeserialize,
  _createAgentVersionFromCodeSend: () => _createAgentVersionFromCodeSend,
  _createOptimizationJobDeserialize: () => _createOptimizationJobDeserialize,
  _createOptimizationJobSend: () => _createOptimizationJobSend,
  _createSessionDeserialize: () => _createSessionDeserialize,
  _createSessionSend: () => _createSessionSend,
  _deleteOptimizationJobDeserialize: () => _deleteOptimizationJobDeserialize,
  _deleteOptimizationJobSend: () => _deleteOptimizationJobSend,
  _deleteSessionDeserialize: () => _deleteSessionDeserialize,
  _deleteSessionFileDeserialize: () => _deleteSessionFileDeserialize,
  _deleteSessionFileSend: () => _deleteSessionFileSend,
  _deleteSessionSend: () => _deleteSessionSend,
  _downloadAgentCodeDeserialize: () => _downloadAgentCodeDeserialize,
  _downloadAgentCodeSend: () => _downloadAgentCodeSend,
  _downloadSessionFileDeserialize: () => _downloadSessionFileDeserialize,
  _downloadSessionFileSend: () => _downloadSessionFileSend,
  _getCandidateFileDeserialize: () => _getCandidateFileDeserialize,
  _getCandidateFileSend: () => _getCandidateFileSend,
  _getOptimizationCandidateConfigDeserialize: () => _getOptimizationCandidateConfigDeserialize,
  _getOptimizationCandidateConfigSend: () => _getOptimizationCandidateConfigSend,
  _getOptimizationCandidateDeserialize: () => _getOptimizationCandidateDeserialize,
  _getOptimizationCandidateResultsDeserialize: () => _getOptimizationCandidateResultsDeserialize,
  _getOptimizationCandidateResultsSend: () => _getOptimizationCandidateResultsSend,
  _getOptimizationCandidateSend: () => _getOptimizationCandidateSend,
  _getOptimizationJobDeserialize: () => _getOptimizationJobDeserialize,
  _getOptimizationJobSend: () => _getOptimizationJobSend,
  _getSessionDeserialize: () => _getSessionDeserialize,
  _getSessionLogStreamDeserialize: () => _getSessionLogStreamDeserialize,
  _getSessionLogStreamSend: () => _getSessionLogStreamSend,
  _getSessionSend: () => _getSessionSend,
  _listOptimizationCandidatesDeserialize: () => _listOptimizationCandidatesDeserialize,
  _listOptimizationCandidatesSend: () => _listOptimizationCandidatesSend,
  _listOptimizationJobsDeserialize: () => _listOptimizationJobsDeserialize,
  _listOptimizationJobsSend: () => _listOptimizationJobsSend,
  _listSessionFilesDeserialize: () => _listSessionFilesDeserialize,
  _listSessionFilesSend: () => _listSessionFilesSend,
  _listSessionsDeserialize: () => _listSessionsDeserialize,
  _listSessionsSend: () => _listSessionsSend,
  _patchAgentObjectDeserialize: () => _patchAgentObjectDeserialize,
  _patchAgentObjectSend: () => _patchAgentObjectSend,
  _promoteCandidateDeserialize: () => _promoteCandidateDeserialize,
  _promoteCandidateSend: () => _promoteCandidateSend,
  _stopSessionDeserialize: () => _stopSessionDeserialize,
  _stopSessionSend: () => _stopSessionSend,
  _uploadSessionFileDeserialize: () => _uploadSessionFileDeserialize,
  _uploadSessionFileSend: () => _uploadSessionFileSend,
  cancelOptimizationJob: () => cancelOptimizationJob,
  createOptimizationJob: () => createOptimizationJob,
  createSession: () => createSession,
  createVersionFromCode: () => createVersionFromCode,
  deleteOptimizationJob: () => deleteOptimizationJob,
  deleteSession: () => deleteSession,
  deleteSessionFile: () => deleteSessionFile,
  downloadAgentCode: () => downloadAgentCode,
  downloadSessionFile: () => downloadSessionFile,
  getCandidateFile: () => getCandidateFile,
  getOptimizationCandidate: () => getOptimizationCandidate,
  getOptimizationCandidateConfig: () => getOptimizationCandidateConfig,
  getOptimizationCandidateResults: () => getOptimizationCandidateResults,
  getOptimizationJob: () => getOptimizationJob,
  getSession: () => getSession,
  getSessionLogStream: () => getSessionLogStream,
  listOptimizationCandidates: () => listOptimizationCandidates,
  listOptimizationJobs: () => listOptimizationJobs,
  listSessionFiles: () => listSessionFiles,
  listSessions: () => listSessions,
  promoteCandidate: () => promoteCandidate,
  stopSession: () => stopSession,
  updateAgentObject: () => updateAgentObject,
  uploadSessionFile: () => uploadSessionFile
});
module.exports = __toCommonJS(operations_exports);
var import_models = require("../../../models/models.js");
var import_pagingHelpers = require("../../../static-helpers/pagingHelpers.js");
var import_get_binary_stream_response = require("../../../static-helpers/serialization/get-binary-stream-response.js");
var import_urlTemplate = require("../../../static-helpers/urlTemplate.js");
var import_core_client = require("@azure-rest/core-client");
function _promoteCandidateSend(context, jobId, candidateId, candidateRequest, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs/{jobId}/candidates/{candidateId}:promote{?api-version}",
    {
      jobId,
      candidateId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: (0, import_models.promoteCandidateRequestSerializer)(candidateRequest)
  });
}
async function _promoteCandidateDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.promoteCandidateResponseDeserializer)(result.body);
}
async function promoteCandidate(context, jobId, candidateId, candidateRequest, options = { requestOptions: {} }) {
  const result = await _promoteCandidateSend(
    context,
    jobId,
    candidateId,
    candidateRequest,
    options
  );
  return _promoteCandidateDeserialize(result);
}
function _getCandidateFileSend(context, jobId, candidateId, path, options = { requestOptions: {} }) {
  const path_1 = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs/{jobId}/candidates/{candidateId}/files{?path,api-version}",
    {
      jobId,
      candidateId,
      path,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path_1).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": "HostedAgents=V1Preview,AgentEndpoints=V1Preview",
      accept: "application/octet-stream",
      ...options.requestOptions?.headers
    }
  });
}
async function _getCandidateFileDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return { blobBody: result.blobBody, readableStreamBody: result.readableStreamBody };
}
async function getCandidateFile(context, jobId, candidateId, path, options = { requestOptions: {} }) {
  const result = await _getCandidateFileSend(context, jobId, candidateId, path, options);
  return _getCandidateFileDeserialize(result);
}
function _getOptimizationCandidateResultsSend(context, jobId, candidateId, options = { requestOptions: {} }) {
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs/{jobId}/candidates/{candidateId}/results{?api-version}",
    {
      jobId,
      candidateId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": "HostedAgents=V1Preview,AgentEndpoints=V1Preview",
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getOptimizationCandidateResultsDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.candidateResultsDeserializer)(result.body);
}
async function getOptimizationCandidateResults(context, jobId, candidateId, options = { requestOptions: {} }) {
  const result = await _getOptimizationCandidateResultsSend(context, jobId, candidateId, options);
  return _getOptimizationCandidateResultsDeserialize(result);
}
function _getOptimizationCandidateConfigSend(context, jobId, candidateId, options = { requestOptions: {} }) {
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs/{jobId}/candidates/{candidateId}/config{?api-version}",
    {
      jobId,
      candidateId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": "HostedAgents=V1Preview,AgentEndpoints=V1Preview",
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getOptimizationCandidateConfigDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.candidateDeployConfigDeserializer)(result.body);
}
async function getOptimizationCandidateConfig(context, jobId, candidateId, options = { requestOptions: {} }) {
  const result = await _getOptimizationCandidateConfigSend(context, jobId, candidateId, options);
  return _getOptimizationCandidateConfigDeserialize(result);
}
function _getOptimizationCandidateSend(context, jobId, candidateId, options = { requestOptions: {} }) {
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs/{jobId}/candidates/{candidateId}{?api-version}",
    {
      jobId,
      candidateId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": "HostedAgents=V1Preview,AgentEndpoints=V1Preview",
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getOptimizationCandidateDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.candidateMetadataDeserializer)(result.body);
}
async function getOptimizationCandidate(context, jobId, candidateId, options = { requestOptions: {} }) {
  const result = await _getOptimizationCandidateSend(context, jobId, candidateId, options);
  return _getOptimizationCandidateDeserialize(result);
}
function _listOptimizationCandidatesSend(context, jobId, options = { requestOptions: {} }) {
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs/{jobId}/candidates{?limit,order,after,before,api-version}",
    {
      jobId,
      limit: options?.limit,
      order: options?.order,
      after: options?.after,
      before: options?.before,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": "HostedAgents=V1Preview,AgentEndpoints=V1Preview",
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listOptimizationCandidatesDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.agentsPagedResultOptimizationCandidateDeserializer)(result.body);
}
async function listOptimizationCandidates(context, jobId, options = { requestOptions: {} }) {
  const result = await _listOptimizationCandidatesSend(context, jobId, options);
  return _listOptimizationCandidatesDeserialize(result);
}
function _deleteOptimizationJobSend(context, jobId, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs/{jobId}{?force,api-version}",
    {
      jobId,
      force: options?.force,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).delete({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options.requestOptions?.headers
    }
  });
}
async function _deleteOptimizationJobDeserialize(result) {
  const expectedStatuses = ["204"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return;
}
async function deleteOptimizationJob(context, jobId, options = { requestOptions: {} }) {
  const result = await _deleteOptimizationJobSend(context, jobId, options);
  return _deleteOptimizationJobDeserialize(result);
}
function _cancelOptimizationJobSend(context, jobId, options = { requestOptions: {} }) {
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs/{jobId}:cancel{?api-version}",
    {
      jobId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": "HostedAgents=V1Preview,AgentEndpoints=V1Preview",
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _cancelOptimizationJobDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.optimizationJobDeserializer)(result.body);
}
async function cancelOptimizationJob(context, jobId, options = { requestOptions: {} }) {
  const result = await _cancelOptimizationJobSend(context, jobId, options);
  return _cancelOptimizationJobDeserialize(result);
}
function _listOptimizationJobsSend(context, options = { requestOptions: {} }) {
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs{?limit,order,after,before,status,agent_name,api-version}",
    {
      limit: options?.limit,
      order: options?.order,
      after: options?.after,
      before: options?.before,
      status: options?.status,
      agent_name: options?.agentName,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": "HostedAgents=V1Preview,AgentEndpoints=V1Preview",
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listOptimizationJobsDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models._agentsPagedResultOptimizationJobDeserializer)(result.body);
}
function listOptimizationJobs(context, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listOptimizationJobsSend(context, options),
    _listOptimizationJobsDeserialize,
    ["200"],
    { itemName: "data", apiVersion: context.apiVersion }
  );
}
function _getOptimizationJobSend(context, jobId, options = { requestOptions: {} }) {
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs/{jobId}{?api-version}",
    {
      jobId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": "HostedAgents=V1Preview,AgentEndpoints=V1Preview",
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getOptimizationJobDeserialize(result) {
  const expectedStatuses = ["200", "202"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.optimizationJobDeserializer)(result.body);
}
async function getOptimizationJob(context, jobId, options = { requestOptions: {} }) {
  const result = await _getOptimizationJobSend(context, jobId, options);
  return _getOptimizationJobDeserialize(result);
}
function _createOptimizationJobSend(context, inputs, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agent_optimization_jobs{?api-version}",
    {
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.operationId !== void 0 ? { "operation-id": options?.operationId } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: (0, import_models.optimizationJobInputsSerializer)(inputs)
  });
}
async function _createOptimizationJobDeserialize(result) {
  const expectedStatuses = ["201"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.optimizationJobDeserializer)(result.body);
}
async function createOptimizationJob(context, inputs, options = { requestOptions: {} }) {
  const result = await _createOptimizationJobSend(context, inputs, options);
  return _createOptimizationJobDeserialize(result);
}
function _deleteSessionFileSend(context, agentName, agentSessionId, path, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path_1 = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/endpoint/sessions/{agent_session_id}/files{?path,recursive,api-version}",
    {
      agent_name: agentName,
      agent_session_id: agentSessionId,
      path,
      recursive: options?.recursive,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path_1).delete({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.userIsolationKey !== void 0 ? { "x-ms-user-isolation-key": options?.userIsolationKey } : {},
      ...options.requestOptions?.headers
    }
  });
}
async function _deleteSessionFileDeserialize(result) {
  const expectedStatuses = ["204"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return;
}
async function deleteSessionFile(context, agentName, agentSessionId, path, options = { requestOptions: {} }) {
  const result = await _deleteSessionFileSend(context, agentName, agentSessionId, path, options);
  return _deleteSessionFileDeserialize(result);
}
function _listSessionFilesSend(context, agentName, agentSessionId, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/endpoint/sessions/{agent_session_id}/files{?path,limit,order,after,before,api-version}",
    {
      agent_name: agentName,
      agent_session_id: agentSessionId,
      path: options?.path,
      limit: options?.limit,
      order: options?.order,
      after: options?.after,
      before: options?.before,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.userIsolationKey !== void 0 ? { "x-ms-user-isolation-key": options?.userIsolationKey } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listSessionFilesDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.sessionDirectoryListResponseDeserializer)(result.body);
}
function listSessionFiles(context, agentName, agentSessionId, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listSessionFilesSend(context, agentName, agentSessionId, options),
    _listSessionFilesDeserialize,
    ["200"],
    {
      itemName: "entries",
      apiVersion: context.apiVersion,
      cursorFieldName: "last_id",
      hasMoreFieldName: "has_more"
    }
  );
}
function _downloadSessionFileSend(context, agentName, agentSessionId, path, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path_1 = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/endpoint/sessions/{agent_session_id}/files/content{?path,api-version}",
    {
      agent_name: agentName,
      agent_session_id: agentSessionId,
      path,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path_1).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.userIsolationKey !== void 0 ? { "x-ms-user-isolation-key": options?.userIsolationKey } : {},
      accept: "application/octet-stream",
      ...options.requestOptions?.headers
    }
  });
}
async function _downloadSessionFileDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return { blobBody: result.blobBody, readableStreamBody: result.readableStreamBody };
}
async function downloadSessionFile(context, agentName, agentSessionId, path, options = { requestOptions: {} }) {
  const streamableMethod = _downloadSessionFileSend(
    context,
    agentName,
    agentSessionId,
    path,
    options
  );
  const result = await (0, import_get_binary_stream_response.getBinaryStreamResponse)(streamableMethod);
  return _downloadSessionFileDeserialize(result);
}
function _uploadSessionFileSend(context, agentName, agentSessionId, path, content, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path_1 = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/endpoint/sessions/{agent_session_id}/files/content{?path,api-version}",
    {
      agent_name: agentName,
      agent_session_id: agentSessionId,
      path,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path_1).put({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/octet-stream",
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.userIsolationKey !== void 0 ? { "x-ms-user-isolation-key": options?.userIsolationKey } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: content
  });
}
async function _uploadSessionFileDeserialize(result) {
  const expectedStatuses = ["200", "201"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.sessionFileWriteResponseDeserializer)(result.body);
}
async function uploadSessionFile(context, agentName, agentSessionId, path, content, options = { requestOptions: {} }) {
  const result = await _uploadSessionFileSend(
    context,
    agentName,
    agentSessionId,
    path,
    content,
    options
  );
  return _uploadSessionFileDeserialize(result);
}
function _getSessionLogStreamSend(context, agentName, agentVersion, sessionId, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/versions/{agent_version}/sessions/{session_id}:logstream{?api-version}",
    {
      agent_name: agentName,
      agent_version: agentVersion,
      session_id: sessionId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "text/event-stream",
      ...options.requestOptions?.headers
    }
  });
}
async function _getSessionLogStreamDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return { blobBody: result.blobBody, readableStreamBody: result.readableStreamBody };
}
async function getSessionLogStream(context, agentName, agentVersion, sessionId, options = { requestOptions: {} }) {
  const streamableMethod = _getSessionLogStreamSend(
    context,
    agentName,
    agentVersion,
    sessionId,
    options
  );
  const result = await (0, import_get_binary_stream_response.getBinaryStreamResponse)(streamableMethod);
  return _getSessionLogStreamDeserialize(result);
}
function _listSessionsSend(context, agentName, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/endpoint/sessions{?limit,order,after,before,api-version}",
    {
      agent_name: agentName,
      limit: options?.limit,
      order: options?.order,
      after: options?.after,
      before: options?.before,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.userIsolationKey !== void 0 ? { "x-ms-user-isolation-key": options?.userIsolationKey } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listSessionsDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models._agentsPagedResultAgentSessionResourceDeserializer)(result.body);
}
function listSessions(context, agentName, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listSessionsSend(context, agentName, options),
    _listSessionsDeserialize,
    ["200"],
    {
      itemName: "data",
      apiVersion: context.apiVersion,
      nextPageRequestOptions: {
        headers: { "foundry-features": "HostedAgents=V1Preview,AgentEndpoints=V1Preview" }
      },
      cursorFieldName: "last_id",
      hasMoreFieldName: "has_more"
    }
  );
}
function _stopSessionSend(context, agentName, sessionId, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/endpoint/sessions/{session_id}:stop{?api-version}",
    {
      agent_name: agentName,
      session_id: sessionId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options.requestOptions?.headers
    }
  });
}
async function _stopSessionDeserialize(result) {
  const expectedStatuses = ["204"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return;
}
async function stopSession(context, agentName, sessionId, options = { requestOptions: {} }) {
  const result = await _stopSessionSend(context, agentName, sessionId, options);
  return _stopSessionDeserialize(result);
}
function _deleteSessionSend(context, agentName, sessionId, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/endpoint/sessions/{session_id}{?api-version}",
    {
      agent_name: agentName,
      session_id: sessionId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).delete({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.userIsolationKey !== void 0 ? { "x-ms-user-isolation-key": options?.userIsolationKey } : {},
      ...options.requestOptions?.headers
    }
  });
}
async function _deleteSessionDeserialize(result) {
  const expectedStatuses = ["204"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return;
}
async function deleteSession(context, agentName, sessionId, options = { requestOptions: {} }) {
  const result = await _deleteSessionSend(context, agentName, sessionId, options);
  return _deleteSessionDeserialize(result);
}
function _getSessionSend(context, agentName, sessionId, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/endpoint/sessions/{session_id}{?api-version}",
    {
      agent_name: agentName,
      session_id: sessionId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.userIsolationKey !== void 0 ? { "x-ms-user-isolation-key": options?.userIsolationKey } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getSessionDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.agentSessionResourceDeserializer)(result.body);
}
async function getSession(context, agentName, sessionId, options = { requestOptions: {} }) {
  const result = await _getSessionSend(context, agentName, sessionId, options);
  return _getSessionDeserialize(result);
}
function _createSessionSend(context, agentName, versionIndicator, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/endpoint/sessions{?api-version}",
    {
      agent_name: agentName,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.userIsolationKey !== void 0 ? { "x-ms-user-isolation-key": options?.userIsolationKey } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: {
      agent_session_id: options?.agentSessionId,
      version_indicator: (0, import_models.versionIndicatorUnionSerializer)(versionIndicator)
    }
  });
}
async function _createSessionDeserialize(result) {
  const expectedStatuses = ["201"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.agentSessionResourceDeserializer)(result.body);
}
async function createSession(context, agentName, versionIndicator, options = { requestOptions: {} }) {
  const result = await _createSessionSend(context, agentName, versionIndicator, options);
  return _createSessionDeserialize(result);
}
function _downloadAgentCodeSend(context, agentName, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,CodeAgents=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/code:download{?agent_version,api-version}",
    {
      agent_name: agentName,
      agent_version: options?.agentVersion,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/zip",
      ...options.requestOptions?.headers
    }
  });
}
async function _downloadAgentCodeDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    if (result.body?.error) {
      error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    }
    throw error;
  }
  return { blobBody: result.blobBody, readableStreamBody: result.readableStreamBody };
}
async function downloadAgentCode(context, agentName, options = { requestOptions: {} }) {
  const streamableMethod = _downloadAgentCodeSend(context, agentName, options);
  const result = await (0, import_get_binary_stream_response.getBinaryStreamResponse)(streamableMethod);
  return _downloadAgentCodeDeserialize(result);
}
function _createAgentVersionFromCodeSend(context, agentName, codeZipSha256, content, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,CodeAgents=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}/versions{?api-version}",
    {
      agent_name: agentName,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "multipart/form-data",
    headers: {
      "foundry-features": foundryFeatures,
      "x-ms-code-zip-sha256": codeZipSha256,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: (0, import_models.createAgentVersionFromCodeContentSerializer)(content)
  });
}
async function _createAgentVersionFromCodeDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.agentVersionDeserializer)(result.body);
}
async function createVersionFromCode(context, agentName, codeZipSha256, content, options = { requestOptions: {} }) {
  const result = await _createAgentVersionFromCodeSend(
    context,
    agentName,
    codeZipSha256,
    content,
    options
  );
  return _createAgentVersionFromCodeDeserialize(result);
}
function _patchAgentObjectSend(context, agentName, options = { requestOptions: {} }) {
  const foundryFeatures = "HostedAgents=V1Preview,AgentEndpoints=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/agents/{agent_name}{?api-version}",
    {
      agent_name: agentName,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).patch({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/merge-patch+json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: {
      agent_endpoint: !options?.agentEndpoint ? options?.agentEndpoint : (0, import_models.agentEndpointConfigSerializer)(options?.agentEndpoint),
      agent_card: !options?.agentCard ? options?.agentCard : (0, import_models.agentCardSerializer)(options?.agentCard)
    }
  });
}
async function _patchAgentObjectDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.agentDeserializer)(result.body);
}
async function updateAgentObject(context, agentName, options = { requestOptions: {} }) {
  const result = await _patchAgentObjectSend(context, agentName, options);
  return _patchAgentObjectDeserialize(result);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  _cancelOptimizationJobDeserialize,
  _cancelOptimizationJobSend,
  _createAgentVersionFromCodeDeserialize,
  _createAgentVersionFromCodeSend,
  _createOptimizationJobDeserialize,
  _createOptimizationJobSend,
  _createSessionDeserialize,
  _createSessionSend,
  _deleteOptimizationJobDeserialize,
  _deleteOptimizationJobSend,
  _deleteSessionDeserialize,
  _deleteSessionFileDeserialize,
  _deleteSessionFileSend,
  _deleteSessionSend,
  _downloadAgentCodeDeserialize,
  _downloadAgentCodeSend,
  _downloadSessionFileDeserialize,
  _downloadSessionFileSend,
  _getCandidateFileDeserialize,
  _getCandidateFileSend,
  _getOptimizationCandidateConfigDeserialize,
  _getOptimizationCandidateConfigSend,
  _getOptimizationCandidateDeserialize,
  _getOptimizationCandidateResultsDeserialize,
  _getOptimizationCandidateResultsSend,
  _getOptimizationCandidateSend,
  _getOptimizationJobDeserialize,
  _getOptimizationJobSend,
  _getSessionDeserialize,
  _getSessionLogStreamDeserialize,
  _getSessionLogStreamSend,
  _getSessionSend,
  _listOptimizationCandidatesDeserialize,
  _listOptimizationCandidatesSend,
  _listOptimizationJobsDeserialize,
  _listOptimizationJobsSend,
  _listSessionFilesDeserialize,
  _listSessionFilesSend,
  _listSessionsDeserialize,
  _listSessionsSend,
  _patchAgentObjectDeserialize,
  _patchAgentObjectSend,
  _promoteCandidateDeserialize,
  _promoteCandidateSend,
  _stopSessionDeserialize,
  _stopSessionSend,
  _uploadSessionFileDeserialize,
  _uploadSessionFileSend,
  cancelOptimizationJob,
  createOptimizationJob,
  createSession,
  createVersionFromCode,
  deleteOptimizationJob,
  deleteSession,
  deleteSessionFile,
  downloadAgentCode,
  downloadSessionFile,
  getCandidateFile,
  getOptimizationCandidate,
  getOptimizationCandidateConfig,
  getOptimizationCandidateResults,
  getOptimizationJob,
  getSession,
  getSessionLogStream,
  listOptimizationCandidates,
  listOptimizationJobs,
  listSessionFiles,
  listSessions,
  promoteCandidate,
  stopSession,
  updateAgentObject,
  uploadSessionFile
});
//# sourceMappingURL=operations.js.map
