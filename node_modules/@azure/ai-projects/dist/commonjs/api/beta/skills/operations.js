var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var operations_exports = {};
__export(operations_exports, {
  $delete: () => $delete,
  _$deleteDeserialize: () => _$deleteDeserialize,
  _$deleteSend: () => _$deleteSend,
  _createDeserialize: () => _createDeserialize,
  _createFromFilesDeserialize: () => _createFromFilesDeserialize,
  _createFromFilesSend: () => _createFromFilesSend,
  _createSend: () => _createSend,
  _deleteVersionDeserialize: () => _deleteVersionDeserialize,
  _deleteVersionSend: () => _deleteVersionSend,
  _downloadDeserialize: () => _downloadDeserialize,
  _downloadSend: () => _downloadSend,
  _downloadVersionDeserialize: () => _downloadVersionDeserialize,
  _downloadVersionSend: () => _downloadVersionSend,
  _getDeserialize: () => _getDeserialize,
  _getSend: () => _getSend,
  _getVersionDeserialize: () => _getVersionDeserialize,
  _getVersionSend: () => _getVersionSend,
  _listDeserialize: () => _listDeserialize,
  _listSend: () => _listSend,
  _listVersionsDeserialize: () => _listVersionsDeserialize,
  _listVersionsSend: () => _listVersionsSend,
  _updateDeserialize: () => _updateDeserialize,
  _updateSend: () => _updateSend,
  create: () => create,
  createFromFiles: () => createFromFiles,
  deleteVersion: () => deleteVersion,
  download: () => download,
  downloadVersion: () => downloadVersion,
  get: () => get,
  getVersion: () => getVersion,
  list: () => list,
  listVersions: () => listVersions,
  update: () => update
});
module.exports = __toCommonJS(operations_exports);
var import_models = require("../../../models/models.js");
var import_pagingHelpers = require("../../../static-helpers/pagingHelpers.js");
var import_get_binary_stream_response = require("../../../static-helpers/serialization/get-binary-stream-response.js");
var import_urlTemplate = require("../../../static-helpers/urlTemplate.js");
var import_core_client = require("@azure-rest/core-client");
function _$deleteSend(context, name, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).delete({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _$deleteDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.deleteSkillResponseDeserializer)(result.body);
}
async function $delete(context, name, options = { requestOptions: {} }) {
  const result = await _$deleteSend(context, name, options);
  return _$deleteDeserialize(result);
}
function _updateSend(context, name, defaultVersion, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: { default_version: defaultVersion }
  });
}
async function _updateDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.skillDeserializer)(result.body);
}
async function update(context, name, defaultVersion, options = { requestOptions: {} }) {
  const result = await _updateSend(context, name, defaultVersion, options);
  return _updateDeserialize(result);
}
function _listSend(context, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills{?limit,order,after,before,api-version}",
    {
      limit: options?.limit,
      order: options?.order,
      after: options?.after,
      before: options?.before,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models._agentsPagedResultSkillDeserializer)(result.body);
}
function list(context, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listSend(context, options),
    _listDeserialize,
    ["200"],
    {
      itemName: "data",
      apiVersion: context.apiVersion,
      nextPageRequestOptions: { headers: { "foundry-features": "Skills=V1Preview" } },
      cursorFieldName: "last_id",
      hasMoreFieldName: "has_more"
    }
  );
}
function _downloadVersionSend(context, name, version, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}/versions/{version}/content{?api-version}",
    {
      name,
      version,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/zip",
      ...options.requestOptions?.headers
    }
  });
}
async function _downloadVersionDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return { blobBody: result.blobBody, readableStreamBody: result.readableStreamBody };
}
async function downloadVersion(context, name, version, options = { requestOptions: {} }) {
  const streamableMethod = _downloadVersionSend(context, name, version, options);
  const result = await (0, import_get_binary_stream_response.getBinaryStreamResponse)(streamableMethod);
  return _downloadVersionDeserialize(result);
}
function _downloadSend(context, name, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}/content{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/zip",
      ...options.requestOptions?.headers
    }
  });
}
async function _downloadDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return { blobBody: result.blobBody, readableStreamBody: result.readableStreamBody };
}
async function download(context, name, options = { requestOptions: {} }) {
  const streamableMethod = _downloadSend(context, name, options);
  const result = await (0, import_get_binary_stream_response.getBinaryStreamResponse)(streamableMethod);
  return _downloadDeserialize(result);
}
function _getSend(context, name, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.skillDeserializer)(result.body);
}
async function get(context, name, options = { requestOptions: {} }) {
  const result = await _getSend(context, name, options);
  return _getDeserialize(result);
}
function _getVersionSend(context, name, version, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}/versions/{version}{?api-version}",
    {
      name,
      version,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getVersionDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.skillVersionDeserializer)(result.body);
}
async function getVersion(context, name, version, options = { requestOptions: {} }) {
  const result = await _getVersionSend(context, name, version, options);
  return _getVersionDeserialize(result);
}
function _listVersionsSend(context, name, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}/versions{?limit,order,after,before,api-version}",
    {
      name,
      limit: options?.limit,
      order: options?.order,
      after: options?.after,
      before: options?.before,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listVersionsDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models._agentsPagedResultSkillVersionDeserializer)(result.body);
}
function listVersions(context, name, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listVersionsSend(context, name, options),
    _listVersionsDeserialize,
    ["200"],
    { itemName: "data", apiVersion: context.apiVersion }
  );
}
function _createFromFilesSend(context, name, content, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}/versions{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "multipart/form-data",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: (0, import_models.createSkillVersionFromFilesBodySerializer)(content)
  });
}
async function _createFromFilesDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.skillVersionDeserializer)(result.body);
}
async function createFromFiles(context, name, content, options = { requestOptions: {} }) {
  const result = await _createFromFilesSend(context, name, content, options);
  return _createFromFilesDeserialize(result);
}
function _createSend(context, name, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}/versions{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: {
      inline_content: !options?.inlineContent ? options?.inlineContent : (0, import_models.skillInlineContentSerializer)(options?.inlineContent),
      default: options?.defaultParam
    }
  });
}
async function _createDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.skillVersionDeserializer)(result.body);
}
async function create(context, name, options = { requestOptions: {} }) {
  const result = await _createSend(context, name, options);
  return _createDeserialize(result);
}
function _deleteVersionSend(context, name, version, options = { requestOptions: {} }) {
  const foundryFeatures = "Skills=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/skills/{name}/versions/{version}{?api-version}",
    {
      name,
      version,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).delete({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _deleteVersionDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.deleteSkillVersionResponseDeserializer)(result.body);
}
async function deleteVersion(context, name, version, options = { requestOptions: {} }) {
  const result = await _deleteVersionSend(context, name, version, options);
  return _deleteVersionDeserialize(result);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  $delete,
  _$deleteDeserialize,
  _$deleteSend,
  _createDeserialize,
  _createFromFilesDeserialize,
  _createFromFilesSend,
  _createSend,
  _deleteVersionDeserialize,
  _deleteVersionSend,
  _downloadDeserialize,
  _downloadSend,
  _downloadVersionDeserialize,
  _downloadVersionSend,
  _getDeserialize,
  _getSend,
  _getVersionDeserialize,
  _getVersionSend,
  _listDeserialize,
  _listSend,
  _listVersionsDeserialize,
  _listVersionsSend,
  _updateDeserialize,
  _updateSend,
  create,
  createFromFiles,
  deleteVersion,
  download,
  downloadVersion,
  get,
  getVersion,
  list,
  listVersions,
  update
});
//# sourceMappingURL=operations.js.map
