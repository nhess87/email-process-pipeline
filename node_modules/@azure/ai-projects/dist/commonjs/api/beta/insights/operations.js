var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var operations_exports = {};
__export(operations_exports, {
  _generateDeserialize: () => _generateDeserialize,
  _generateSend: () => _generateSend,
  _getDeserialize: () => _getDeserialize,
  _getSend: () => _getSend,
  _listDeserialize: () => _listDeserialize,
  _listSend: () => _listSend,
  generate: () => generate,
  get: () => get,
  list: () => list
});
module.exports = __toCommonJS(operations_exports);
var import_models = require("../../../models/models.js");
var import_pagingHelpers = require("../../../static-helpers/pagingHelpers.js");
var import_urlTemplate = require("../../../static-helpers/urlTemplate.js");
var import_core_client = require("@azure-rest/core-client");
function _listSend(context, options = { requestOptions: {} }) {
  const foundryFeatures = "Insights=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/insights{?api-version,type,evalId,runId,agentName,includeCoordinates}",
    {
      "api-version": context.apiVersion,
      type: options?.insightType,
      evalId: options?.evalId,
      runId: options?.runId,
      agentName: options?.agentName,
      includeCoordinates: options?.includeCoordinates
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.clientRequestId !== void 0 ? { "x-ms-client-request-id": options?.clientRequestId } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models._pagedInsightDeserializer)(result.body);
}
function list(context, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listSend(context, options),
    _listDeserialize,
    ["200"],
    {
      itemName: "value",
      nextLinkName: "nextLink",
      apiVersion: context.apiVersion,
      nextPageRequestOptions: {
        headers: {
          "foundry-features": "Insights=V1Preview"
        }
      }
    }
  );
}
function _getSend(context, insightId, options = { requestOptions: {} }) {
  const foundryFeatures = "Insights=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/insights/{id}{?api-version,includeCoordinates}",
    {
      id: insightId,
      includeCoordinates: options?.includeCoordinates,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.clientRequestId !== void 0 ? { "x-ms-client-request-id": options?.clientRequestId } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.insightDeserializer)(result.body);
}
async function get(context, insightId, options = { requestOptions: {} }) {
  const result = await _getSend(context, insightId, options);
  return _getDeserialize(result);
}
function _generateSend(context, insight, options = { requestOptions: {} }) {
  const foundryFeatures = "Insights=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/insights{?api-version}",
    {
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.repeatabilityRequestId !== void 0 ? { "repeatability-request-id": options?.repeatabilityRequestId } : {},
      ...options?.repeatabilityFirstSent !== void 0 ? {
        "repeatability-first-sent": !options?.repeatabilityFirstSent ? options?.repeatabilityFirstSent : options?.repeatabilityFirstSent.toUTCString()
      } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: {
      displayName: insight.displayName,
      request: (0, import_models.insightRequestUnionSerializer)(insight.request)
    }
  });
}
async function _generateDeserialize(result) {
  const expectedStatuses = ["201"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.insightDeserializer)(result.body);
}
async function generate(context, insight, options = { requestOptions: {} }) {
  const result = await _generateSend(context, insight, options);
  return _generateDeserialize(result);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  _generateDeserialize,
  _generateSend,
  _getDeserialize,
  _getSend,
  _listDeserialize,
  _listSend,
  generate,
  get,
  list
});
//# sourceMappingURL=operations.js.map
