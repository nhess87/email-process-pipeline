var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var operations_exports = {};
__export(operations_exports, {
  $delete: () => $delete,
  _$deleteDeserialize: () => _$deleteDeserialize,
  _$deleteSend: () => _$deleteSend,
  _createOrUpdateDeserialize: () => _createOrUpdateDeserialize,
  _createOrUpdateSend: () => _createOrUpdateSend,
  _getDeserialize: () => _getDeserialize,
  _getRunDeserialize: () => _getRunDeserialize,
  _getRunSend: () => _getRunSend,
  _getSend: () => _getSend,
  _listDeserialize: () => _listDeserialize,
  _listRunsDeserialize: () => _listRunsDeserialize,
  _listRunsSend: () => _listRunsSend,
  _listSend: () => _listSend,
  createOrUpdate: () => createOrUpdate,
  get: () => get,
  getRun: () => getRun,
  list: () => list,
  listRuns: () => listRuns
});
module.exports = __toCommonJS(operations_exports);
var import_models = require("../../../models/models.js");
var import_pagingHelpers = require("../../../static-helpers/pagingHelpers.js");
var import_urlTemplate = require("../../../static-helpers/urlTemplate.js");
var import_core_client = require("@azure-rest/core-client");
function _listRunsSend(context, scheduleId, options = { requestOptions: {} }) {
  const foundryFeatures = "Schedules=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/schedules/{id}/runs{?api-version,type,enabled}",
    {
      id: scheduleId,
      "api-version": context.apiVersion,
      type: options?.scheduleType,
      enabled: options?.enabled
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.clientRequestId !== void 0 ? { "x-ms-client-request-id": options?.clientRequestId } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listRunsDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models._pagedScheduleRunDeserializer)(result.body);
}
function listRuns(context, scheduleId, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listRunsSend(context, scheduleId, options),
    _listRunsDeserialize,
    ["200"],
    {
      itemName: "value",
      nextLinkName: "nextLink",
      apiVersion: context.apiVersion,
      nextPageRequestOptions: {
        headers: {
          "foundry-features": "Schedules=V1Preview"
        }
      }
    }
  );
}
function _getRunSend(context, scheduleId, runId, options = { requestOptions: {} }) {
  const foundryFeatures = "Schedules=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/schedules/{schedule_id}/runs/{run_id}{?api-version}",
    {
      schedule_id: scheduleId,
      run_id: runId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getRunDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.scheduleRunDeserializer)(result.body);
}
async function getRun(context, scheduleId, runId, options = { requestOptions: {} }) {
  const result = await _getRunSend(context, scheduleId, runId, options);
  return _getRunDeserialize(result);
}
function _createOrUpdateSend(context, scheduleId, schedule, options = { requestOptions: {} }) {
  const foundryFeatures = "Schedules=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/schedules/{id}{?api-version}",
    {
      id: scheduleId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).put({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.clientRequestId !== void 0 ? { "x-ms-client-request-id": options?.clientRequestId } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: (0, import_models.scheduleSerializer)(schedule)
  });
}
async function _createOrUpdateDeserialize(result) {
  const expectedStatuses = ["201", "200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models.scheduleDeserializer)(result.body);
}
async function createOrUpdate(context, scheduleId, schedule, options = { requestOptions: {} }) {
  const result = await _createOrUpdateSend(context, scheduleId, schedule, options);
  return _createOrUpdateDeserialize(result);
}
function _listSend(context, options = { requestOptions: {} }) {
  const foundryFeatures = "Schedules=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/schedules{?api-version,type,enabled}",
    {
      "api-version": context.apiVersion,
      type: options?.scheduleType,
      enabled: options?.enabled
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.clientRequestId !== void 0 ? { "x-ms-client-request-id": options?.clientRequestId } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models._pagedScheduleDeserializer)(result.body);
}
function list(context, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listSend(context, options),
    _listDeserialize,
    ["200"],
    {
      itemName: "value",
      nextLinkName: "nextLink",
      apiVersion: context.apiVersion,
      nextPageRequestOptions: {
        headers: {
          "foundry-features": "Schedules=V1Preview"
        }
      }
    }
  );
}
function _getSend(context, scheduleId, options = { requestOptions: {} }) {
  const foundryFeatures = "Schedules=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/schedules/{id}{?api-version}",
    {
      id: scheduleId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.clientRequestId !== void 0 ? { "x-ms-client-request-id": options?.clientRequestId } : {},
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return (0, import_models.scheduleDeserializer)(result.body);
}
async function get(context, scheduleId, options = { requestOptions: {} }) {
  const result = await _getSend(context, scheduleId, options);
  return _getDeserialize(result);
}
function _$deleteSend(context, scheduleId, options = { requestOptions: {} }) {
  const foundryFeatures = "Schedules=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/schedules/{id}{?api-version}",
    {
      id: scheduleId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).delete({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      ...options?.clientRequestId !== void 0 ? { "x-ms-client-request-id": options?.clientRequestId } : {},
      ...options.requestOptions?.headers
    }
  });
}
async function _$deleteDeserialize(result) {
  const expectedStatuses = ["204"];
  if (!expectedStatuses.includes(result.status)) {
    throw (0, import_core_client.createRestError)(result);
  }
  return;
}
async function $delete(context, scheduleId, options = { requestOptions: {} }) {
  const result = await _$deleteSend(context, scheduleId, options);
  return _$deleteDeserialize(result);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  $delete,
  _$deleteDeserialize,
  _$deleteSend,
  _createOrUpdateDeserialize,
  _createOrUpdateSend,
  _getDeserialize,
  _getRunDeserialize,
  _getRunSend,
  _getSend,
  _listDeserialize,
  _listRunsDeserialize,
  _listRunsSend,
  _listSend,
  createOrUpdate,
  get,
  getRun,
  list,
  listRuns
});
//# sourceMappingURL=operations.js.map
