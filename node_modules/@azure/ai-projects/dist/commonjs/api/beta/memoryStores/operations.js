var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var operations_exports = {};
__export(operations_exports, {
  $delete: () => $delete,
  _createDeserialize: () => _createDeserialize,
  _createMemoryDeserialize: () => _createMemoryDeserialize,
  _createMemorySend: () => _createMemorySend,
  _createSend: () => _createSend,
  _deleteDeserialize: () => _deleteDeserialize,
  _deleteMemoryDeserialize: () => _deleteMemoryDeserialize,
  _deleteMemorySend: () => _deleteMemorySend,
  _deleteScopeDeserialize: () => _deleteScopeDeserialize,
  _deleteScopeSend: () => _deleteScopeSend,
  _deleteSend: () => _deleteSend,
  _getDeserialize: () => _getDeserialize,
  _getMemoryDeserialize: () => _getMemoryDeserialize,
  _getMemorySend: () => _getMemorySend,
  _getSend: () => _getSend,
  _getUpdateResultDeserialize: () => _getUpdateResultDeserialize,
  _getUpdateResultSend: () => _getUpdateResultSend,
  _listDeserialize: () => _listDeserialize,
  _listMemoriesDeserialize: () => _listMemoriesDeserialize,
  _listMemoriesSend: () => _listMemoriesSend,
  _listSend: () => _listSend,
  _searchMemoriesDeserialize: () => _searchMemoriesDeserialize,
  _searchMemoriesSend: () => _searchMemoriesSend,
  _updateDeserialize: () => _updateDeserialize,
  _updateMemoriesDeserialize: () => _updateMemoriesDeserialize,
  _updateMemoriesSend: () => _updateMemoriesSend,
  _updateMemoryDeserialize: () => _updateMemoryDeserialize,
  _updateMemorySend: () => _updateMemorySend,
  _updateSend: () => _updateSend,
  create: () => create,
  createMemory: () => createMemory,
  deleteMemory: () => deleteMemory,
  deleteScope: () => deleteScope,
  get: () => get,
  getMemory: () => getMemory,
  getUpdateResult: () => getUpdateResult,
  list: () => list,
  listMemories: () => listMemories,
  searchMemories: () => searchMemories,
  update: () => update,
  updateMemories: () => updateMemories,
  updateMemory: () => updateMemory
});
module.exports = __toCommonJS(operations_exports);
var import_models = require("../../../models/models.js");
var import_pagingHelpers = require("../../../static-helpers/pagingHelpers.js");
var import_pollingHelpers = require("../../../static-helpers/pollingHelpers.js");
var import_urlTemplate = require("../../../static-helpers/urlTemplate.js");
var import_core_client = require("@azure-rest/core-client");
function _deleteMemorySend(context, name, memoryId, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}/items/{memory_id}{?api-version}",
    {
      name,
      memory_id: memoryId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).delete({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _deleteMemoryDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.deleteMemoryResponseDeserializer)(result.body);
}
async function deleteMemory(context, name, memoryId, options = { requestOptions: {} }) {
  const result = await _deleteMemorySend(context, name, memoryId, options);
  return _deleteMemoryDeserialize(result);
}
function _listMemoriesSend(context, name, scope, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}/items:list{?kind,limit,order,after,before,api-version}",
    {
      name,
      kind: options?.kind,
      limit: options?.limit,
      order: options?.order,
      after: options?.after,
      before: options?.before,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: { scope }
  });
}
async function _listMemoriesDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models._agentsPagedResultMemoryItemDeserializer)(result.body);
}
function listMemories(context, name, scope, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listMemoriesSend(context, name, scope, options),
    _listMemoriesDeserialize,
    ["200"],
    { itemName: "data", apiVersion: context.apiVersion }
  );
}
function _getMemorySend(context, name, memoryId, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}/items/{memory_id}{?api-version}",
    {
      name,
      memory_id: memoryId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getMemoryDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.memoryItemUnionDeserializer)(result.body);
}
async function getMemory(context, name, memoryId, options = { requestOptions: {} }) {
  const result = await _getMemorySend(context, name, memoryId, options);
  return _getMemoryDeserialize(result);
}
function _updateMemorySend(context, name, memoryId, content, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}/items/{memory_id}{?api-version}",
    {
      name,
      memory_id: memoryId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: { content }
  });
}
async function _updateMemoryDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.memoryItemUnionDeserializer)(result.body);
}
async function updateMemory(context, name, memoryId, content, options = { requestOptions: {} }) {
  const result = await _updateMemorySend(context, name, memoryId, content, options);
  return _updateMemoryDeserialize(result);
}
function _createMemorySend(context, name, scope, content, kind, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}/items{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: { scope, content, kind }
  });
}
async function _createMemoryDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.memoryItemUnionDeserializer)(result.body);
}
async function createMemory(context, name, scope, content, kind, options = { requestOptions: {} }) {
  const result = await _createMemorySend(context, name, scope, content, kind, options);
  return _createMemoryDeserialize(result);
}
function _deleteScopeSend(context, name, scope, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}:delete_scope{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: { scope }
  });
}
async function _deleteScopeDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.memoryStoreDeleteScopeResponseDeserializer)(result.body);
}
async function deleteScope(context, name, scope, options = { requestOptions: {} }) {
  const result = await _deleteScopeSend(context, name, scope, options);
  return _deleteScopeDeserialize(result);
}
function _getUpdateResultSend(context, name, updateId, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}/updates/{update_id}{?api-version}",
    {
      name,
      update_id: updateId,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getUpdateResultDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.memoryStoreUpdateResponseDeserializer)(result.body);
}
async function getUpdateResult(context, name, updateId, options = { requestOptions: {} }) {
  const result = await _getUpdateResultSend(context, name, updateId, options);
  return _getUpdateResultDeserialize(result);
}
function _updateMemoriesSend(context, name, scope, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}:update_memories{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: {
      scope,
      items: !options?.items ? options?.items : options?.items.map((p) => {
        return p;
      }),
      previous_update_id: options?.previousUpdateId,
      update_delay: options?.updateDelayInSecs
    }
  });
}
async function _updateMemoriesDeserialize(result) {
  const expectedStatuses = ["202", "200", "201"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  if (result?.body?.result === void 0) {
    throw (0, import_core_client.createRestError)(
      `Expected a result in the response at position "result.body.result"`,
      result
    );
  }
  return (0, import_models.memoryStoreUpdateCompletedResultDeserializer)(result.body.result);
}
function updateMemories(context, name, scope, options = { requestOptions: {} }) {
  return (0, import_pollingHelpers.getLongRunningPoller)(context, _updateMemoriesDeserialize, ["202", "200", "201"], {
    updateIntervalInMs: options?.updateIntervalInMs,
    abortSignal: options?.abortSignal,
    getInitialResponse: () => _updateMemoriesSend(context, name, scope, options),
    apiVersion: context.apiVersion,
    pollHeaders: {
      ...options?.requestOptions?.headers,
      "foundry-features": "MemoryStores=V1Preview"
    },
    statusNormalizations: {
      queued: "running",
      in_progress: "running",
      completed: "succeeded",
      failed: "failed",
      superseded: "canceled"
    }
  });
}
function _searchMemoriesSend(context, name, scope, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}:search_memories{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: {
      scope,
      items: !options?.items ? options?.items : options?.items.map((p) => {
        return p;
      }),
      previous_search_id: options?.previousSearchId,
      options: !options?.options ? options?.options : (0, import_models.memorySearchOptionsSerializer)(options?.options)
    }
  });
}
async function _searchMemoriesDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.memoryStoreSearchResponseDeserializer)(result.body);
}
async function searchMemories(context, name, scope, options = { requestOptions: {} }) {
  const result = await _searchMemoriesSend(context, name, scope, options);
  return _searchMemoriesDeserialize(result);
}
function _deleteSend(context, name, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).delete({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _deleteDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.deleteMemoryStoreResponseDeserializer)(result.body);
}
async function $delete(context, name, options = { requestOptions: {} }) {
  const result = await _deleteSend(context, name, options);
  return _deleteDeserialize(result);
}
function _listSend(context, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores{?limit,order,after,before,api-version}",
    {
      limit: options?.limit,
      order: options?.order,
      after: options?.after,
      before: options?.before,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _listDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models._agentsPagedResultMemoryStoreObjectDeserializer)(result.body);
}
function list(context, options = { requestOptions: {} }) {
  return (0, import_pagingHelpers.buildPagedAsyncIterator)(
    context,
    () => _listSend(context, options),
    _listDeserialize,
    ["200"],
    {
      itemName: "data",
      apiVersion: context.apiVersion,
      nextPageRequestOptions: {
        headers: {
          "foundry-features": "MemoryStores=V1Preview"
        }
      },
      cursorFieldName: "last_id",
      hasMoreFieldName: "has_more"
    }
  );
}
function _getSend(context, name, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).get({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    }
  });
}
async function _getDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.memoryStoreDeserializer)(result.body);
}
async function get(context, name, options = { requestOptions: {} }) {
  const result = await _getSend(context, name, options);
  return _getDeserialize(result);
}
function _updateSend(context, name, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores/{name}{?api-version}",
    {
      name,
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: { description: options?.description, metadata: options?.metadata }
  });
}
async function _updateDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.memoryStoreDeserializer)(result.body);
}
async function update(context, name, options = { requestOptions: {} }) {
  const result = await _updateSend(context, name, options);
  return _updateDeserialize(result);
}
function _createSend(context, name, definition, options = { requestOptions: {} }) {
  const foundryFeatures = "MemoryStores=V1Preview";
  const path = (0, import_urlTemplate.expandUrlTemplate)(
    "/memory_stores{?api-version}",
    {
      "api-version": context.apiVersion
    },
    {
      allowReserved: options?.requestOptions?.skipUrlEncoding
    }
  );
  return context.path(path).post({
    ...(0, import_core_client.operationOptionsToRequestParameters)(options),
    contentType: "application/json",
    headers: {
      "foundry-features": foundryFeatures,
      accept: "application/json",
      ...options.requestOptions?.headers
    },
    body: {
      name,
      description: options?.description,
      metadata: options?.metadata,
      definition: (0, import_models.memoryStoreDefinitionUnionSerializer)(definition)
    }
  });
}
async function _createDeserialize(result) {
  const expectedStatuses = ["200"];
  if (!expectedStatuses.includes(result.status)) {
    const error = (0, import_core_client.createRestError)(result);
    error.details = (0, import_models.apiErrorResponseDeserializer)(result.body);
    throw error;
  }
  return (0, import_models.memoryStoreDeserializer)(result.body);
}
async function create(context, name, definition, options = { requestOptions: {} }) {
  const result = await _createSend(context, name, definition, options);
  return _createDeserialize(result);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  $delete,
  _createDeserialize,
  _createMemoryDeserialize,
  _createMemorySend,
  _createSend,
  _deleteDeserialize,
  _deleteMemoryDeserialize,
  _deleteMemorySend,
  _deleteScopeDeserialize,
  _deleteScopeSend,
  _deleteSend,
  _getDeserialize,
  _getMemoryDeserialize,
  _getMemorySend,
  _getSend,
  _getUpdateResultDeserialize,
  _getUpdateResultSend,
  _listDeserialize,
  _listMemoriesDeserialize,
  _listMemoriesSend,
  _listSend,
  _searchMemoriesDeserialize,
  _searchMemoriesSend,
  _updateDeserialize,
  _updateMemoriesDeserialize,
  _updateMemoriesSend,
  _updateMemoryDeserialize,
  _updateMemorySend,
  _updateSend,
  create,
  createMemory,
  deleteMemory,
  deleteScope,
  get,
  getMemory,
  getUpdateResult,
  list,
  listMemories,
  searchMemories,
  update,
  updateMemories,
  updateMemory
});
//# sourceMappingURL=operations.js.map
