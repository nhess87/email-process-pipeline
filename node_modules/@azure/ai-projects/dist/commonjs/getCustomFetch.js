var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var getCustomFetch_exports = {};
__export(getCustomFetch_exports, {
  getCustomFetch: () => getCustomFetch
});
module.exports = __toCommonJS(getCustomFetch_exports);
var import_core_rest_pipeline = require("@azure/core-rest-pipeline");
var import_node_stream = require("node:stream");
function getCustomFetch(pipeline, customHttpClient) {
  return async function(resource, options) {
    const fetchRequest = new Request(resource, options);
    const rawHeaders = {};
    for (const [key, value] of fetchRequest.headers) {
      rawHeaders[key] = value;
    }
    const httpClient = customHttpClient ?? (0, import_core_rest_pipeline.createDefaultHttpClient)();
    const requestBody = fetchRequest.body ? import_node_stream.Readable.fromWeb(fetchRequest.body) : void 0;
    const request = (0, import_core_rest_pipeline.createPipelineRequest)({
      url: fetchRequest.url,
      body: requestBody,
      headers: (0, import_core_rest_pipeline.createHttpHeaders)(rawHeaders),
      method: fetchRequest.method,
      abortSignal: fetchRequest.signal,
      streamResponseStatusCodes: /* @__PURE__ */ new Set([Number.POSITIVE_INFINITY])
    });
    const response = await pipeline.sendRequest(httpClient, request);
    const body = response.readableStreamBody ? import_node_stream.Readable.toWeb(import_node_stream.Readable.from(response.readableStreamBody)) : null;
    const fetchResponse = new Response(body, {
      status: response.status,
      headers: response.headers.toJSON({ preserveCase: true })
    });
    return fetchResponse;
  };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getCustomFetch
});
//# sourceMappingURL=getCustomFetch.js.map
