var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var aiProjectClient_exports = {};
__export(aiProjectClient_exports, {
  AIProjectClient: () => AIProjectClient
});
module.exports = __toCommonJS(aiProjectClient_exports);
var import_openai = __toESM(require("openai"));
var import_identity = require("@azure/identity");
var import_api = require("./api/index.js");
var import_agents = require("./classic/agents/index.js");
var import_beta = require("./classic/beta/index.js");
var import_connections = require("./classic/connections/index.js");
var import_datasets = require("./classic/datasets/index.js");
var import_deployments = require("./classic/deployments/index.js");
var import_evaluationRules = require("./classic/evaluationRules/index.js");
var import_indexes = require("./classic/indexes/index.js");
var import_telemetry = require("./classic/telemetry/index.js");
var import_overwriteOpenAIClient = require("./overwriteOpenAIClient.js");
var import_getCustomFetch = require("./getCustomFetch.js");
var import_util = require("./util.js");
var import_models = require("./models/models.js");
class AIProjectClient {
  _cognitiveScopeClient;
  _azureScopeClient;
  _endpoint;
  _credential;
  _options;
  constructor(endpoint, credential, options = {}) {
    this._endpoint = endpoint;
    this._credential = credential;
    this._options = options;
    const prefixFromOptions = options?.userAgentOptions?.userAgentPrefix;
    const userAgentPrefix = prefixFromOptions ? `${prefixFromOptions} azsdk-js-client` : `azsdk-js-client`;
    this._cognitiveScopeClient = (0, import_api.createAIProject)(endpoint, this._credential, {
      ...options,
      userAgentOptions: { userAgentPrefix },
      credentials: {
        ...options.credentials,
        scopes: ["https://ai.azure.com/.default"]
      }
    });
    this._azureScopeClient = (0, import_api.createAIProject)(endpoint, credential, {
      ...options,
      userAgentOptions: { userAgentPrefix }
    });
    this.indexes = (0, import_indexes._getIndexesOperations)(this._azureScopeClient);
    this.deployments = (0, import_deployments._getDeploymentsOperations)(this._azureScopeClient);
    this.datasets = (0, import_datasets._getDatasetsOperations)(this._azureScopeClient, this._options);
    this.connections = (0, import_connections._getConnectionsOperations)(this._azureScopeClient);
    this.evaluationRules = (0, import_evaluationRules._getEvaluationRulesOperations)(this._azureScopeClient);
    this.agents = (0, import_agents._getAgentsOperations)(this._azureScopeClient);
    this.beta = (0, import_beta._getBetaOperations)(this._cognitiveScopeClient);
    this.telemetry = (0, import_telemetry._getTelemetryOperations)(this.connections);
  }
  /** The operation groups for indexes */
  indexes;
  /** The operation groups for deployments */
  deployments;
  /** The operation groups for datasets */
  datasets;
  /** The operation groups for connections */
  connections;
  /** The operation groups for evaluationRules */
  evaluationRules;
  /** The operation groups for agents */
  agents;
  /** The operation groups for beta include beta features:
   * - Memory Stores
   * - Evaluators
   * - Evaluation Taxonomies
   * - Insights
   * - Schedules
   * - Red Teams
   * - Toolboxes
   * - agents
   * - skills
   * - routines
   * - models
   */
  beta;
  /** The operation groups for telemetry */
  telemetry;
  /**
   * gets the OpenAI client
   * @returns the OpenAI client
   */
  // eslint-disable-next-line @azure/azure-sdk/ts-naming-options
  getOpenAIClient(optsWithAzureAgent) {
    const scope = "https://ai.azure.com/.default";
    const { azureConfig, ...opts } = optsWithAzureAgent || {};
    let customFetch;
    if (this._options.additionalPolicies?.find((policy) => policy.policy.name === "recording policy")) {
      customFetch = (0, import_getCustomFetch.getCustomFetch)(this._azureScopeClient.pipeline, this._options.httpClient);
    }
    let baseURL;
    if (opts?.baseURL) {
      baseURL = opts.baseURL;
    } else if (azureConfig?.agentName) {
      if (!azureConfig.allowPreview) {
        throw new Error(
          "Calling `getOpenAIClient` with an `agentName` requires you to set `allowPreview: true`\nwhen providing azureConfig. Note that preview features are under development and \nsubject to change. They should not be used in production environments."
        );
      }
      baseURL = `${this._endpoint}/agents/${azureConfig.agentName}/endpoint/protocols/openai`;
    } else {
      baseURL = `${this._endpoint}/openai/v1`;
    }
    const defaultHeaders = (0, import_util.getOpenAIDefaultHeaders)(
      opts?.defaultHeaders,
      this._options?.userAgentOptions?.userAgentPrefix
    );
    if (azureConfig?.agentName && !defaultHeaders.has("foundry-features")) {
      defaultHeaders.set("foundry-features", "HostedAgents=V1Preview,AgentEndpoints=V1Preview");
    }
    const defaultQuery = {
      ...opts?.defaultQuery || {}
    };
    if (azureConfig?.agentName && !defaultQuery["api-version"]) {
      defaultQuery["api-version"] = this._options.apiVersion ?? import_models.KnownApiVersions.v1;
    }
    const {
      defaultHeaders: _ignoredHeaders,
      baseURL: _ignoredBaseURL,
      defaultQuery: _ignoredQuery,
      ...restOpts
    } = opts || {};
    const openAIOptions = {
      ...restOpts,
      apiKey: (0, import_identity.getBearerTokenProvider)(this._credential, scope),
      baseURL,
      dangerouslyAllowBrowser: true,
      defaultHeaders: defaultHeaders.toJSON({ preserveCase: true }),
      defaultQuery: Object.keys(defaultQuery).length > 0 ? defaultQuery : void 0,
      fetch: customFetch
    };
    const openaiClient = new import_openai.default(openAIOptions);
    return (0, import_overwriteOpenAIClient.overwriteOpenAIClient)(openaiClient);
  }
  /**
   * gets the endpoint of the client
   * @returns the endpoint of the client
   */
  get endpoint() {
    return this._endpoint;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AIProjectClient
});
//# sourceMappingURL=aiProjectClient.js.map
