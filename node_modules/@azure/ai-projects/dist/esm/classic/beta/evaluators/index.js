// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { deleteGenerationJob, cancelGenerationJob, listGenerationJobs, getGenerationJob, createGenerationJob, getCredentials, pendingUpload, updateVersion, createVersion, deleteVersion, getVersion, list, listVersions, } from "../../../api/beta/evaluators/operations.js";
function _getBetaEvaluators(context) {
    return {
        deleteGenerationJob: (jobId, options) => deleteGenerationJob(context, jobId, options),
        cancelGenerationJob: (jobId, options) => cancelGenerationJob(context, jobId, options),
        listGenerationJobs: (options) => listGenerationJobs(context, options),
        getGenerationJob: (jobId, options) => getGenerationJob(context, jobId, options),
        createGenerationJob: (job, options) => createGenerationJob(context, job, options),
        getCredentials: (name, credentialRequest, version, options) => getCredentials(context, name, credentialRequest, version, options),
        pendingUpload: (name, version, pendingUploadRequest, options) => pendingUpload(context, name, version, pendingUploadRequest, options),
        updateVersion: (name, version, evaluatorVersion, options) => updateVersion(context, name, version, evaluatorVersion, options),
        createVersion: (name, evaluatorVersion, options) => createVersion(context, name, evaluatorVersion, options),
        deleteVersion: (name, version, options) => deleteVersion(context, name, version, options),
        getVersion: (name, version, options) => getVersion(context, name, version, options),
        list: (options) => list(context, options),
        listVersions: (name, options) => listVersions(context, name, options),
    };
}
export function _getBetaEvaluatorsOperations(context) {
    return {
        ..._getBetaEvaluators(context),
    };
}
//# sourceMappingURL=index.js.map