// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { getCredentials, pendingUpload, createFromSource, update, $delete, get, list, listVersions, } from "../../../api/beta/models/operations.js";
function _getBetaModels(context) {
    return {
        getCredentials: (name, version, credentialRequest, options) => getCredentials(context, name, version, credentialRequest, options),
        pendingUpload: (name, version, pendingUploadRequest, options) => pendingUpload(context, name, version, pendingUploadRequest, options),
        create: (name, version, source, options) => createFromSource(context, name, version, source, options),
        update: (name, body, version, options) => update(context, name, body, version, options),
        delete: (name, version, options) => $delete(context, name, version, options),
        get: (name, version, options) => get(context, name, version, options),
        list: (options) => list(context, options),
        listVersions: (name, options) => listVersions(context, name, options),
    };
}
export function _getBetaModelsOperations(context) {
    return {
        ..._getBetaModels(context),
    };
}
//# sourceMappingURL=index.js.map