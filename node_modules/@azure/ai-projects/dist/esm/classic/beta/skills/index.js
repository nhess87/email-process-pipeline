// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { deleteVersion, downloadVersion, download, getVersion, listVersions, createFromFiles, create, $delete, update, list, get, } from "../../../api/beta/skills/operations.js";
function _getBetaSkills(context) {
    return {
        deleteVersion: (name, version, options) => deleteVersion(context, name, version, options),
        downloadVersion: (name, version, options) => downloadVersion(context, name, version, options),
        download: (name, options) => download(context, name, options),
        getVersion: (name, version, options) => getVersion(context, name, version, options),
        listVersions: (name, options) => listVersions(context, name, options),
        createFromFiles: (name, content, options) => createFromFiles(context, name, content, options),
        create: (name, options) => create(context, name, options),
        delete: (name, options) => $delete(context, name, options),
        update: (name, defaultVersion, options) => update(context, name, defaultVersion, options),
        list: (options) => list(context, options),
        get: (name, options) => get(context, name, options),
    };
}
export function _getBetaSkillsOperations(context) {
    return {
        ..._getBetaSkills(context),
    };
}
//# sourceMappingURL=index.js.map