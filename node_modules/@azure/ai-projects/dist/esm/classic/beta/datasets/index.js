// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { deleteGenerationJob, cancelGenerationJob, createGenerationJob, listGenerationJobs, getGenerationJob, } from "../../../api/beta/datasets/operations.js";
function _getBetaDatasets(context) {
    return {
        deleteGenerationJob: (jobId, options) => deleteGenerationJob(context, jobId, options),
        cancelGenerationJob: (jobId, options) => cancelGenerationJob(context, jobId, options),
        createGenerationJob: (job, options) => createGenerationJob(context, job, options),
        listGenerationJobs: (options) => listGenerationJobs(context, options),
        getGenerationJob: (jobId, options) => getGenerationJob(context, jobId, options),
    };
}
export function _getBetaDatasetsOperations(context) {
    return {
        ..._getBetaDatasets(context),
    };
}
//# sourceMappingURL=index.js.map